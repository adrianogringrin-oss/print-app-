# 🚀 Развертывание на сервере/облаке

Это руководство поможет развернуть приложение на различных платформах для постоянной работы.

## 📋 Содержание

1. [Подготовка](#подготовка)
2. [Railway (рекомендуется)](#railway-рекомендуется)
3. [Render](#render)
4. [VPS (DigitalOcean, Hetzner и т.д.)](#vps-digitalocean-hetzner-ид)
5. [Docker локально](#docker-локально)

---

## Подготовка

### Необходимые данные:

1. **Telegram Bot Token** - получите у [@BotFather](https://t.me/BotFather)
2. **OpenAI API Key** - получите на [platform.openai.com](https://platform.openai.com/api-keys)
3. **Публичный URL** - будет предоставлен платформой развертывания

### Переменные окружения:

```
TELEGRAM_BOT_TOKEN=ваш_токен_бота
OPENAI_API_KEY=ваш_ключ_openai
WEB_APP_URL=https://ваш_домен.com
PORT=8080
```

---

## Railway (рекомендуется)

### Преимущества:
- ✅ Бесплатный тариф (500 часов/месяц)
- ✅ Автоматическое развертывание из GitHub
- ✅ HTTPS из коробки
- ✅ Простая настройка

### Шаги:

1. **Создайте аккаунт на [Railway](https://railway.app/)**

2. **Подключите GitHub репозиторий:**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий

3. **Настройте переменные окружения:**
   - Settings → Variables
   - Добавьте:
     ```
     TELEGRAM_BOT_TOKEN=ваш_токен
     OPENAI_API_KEY=ваш_ключ
     PORT=8080
     ```

4. **Настройте развертывание:**
   - Settings → Deploy
   - Build Command: `pip install -r requirements_telegram.txt`
   - Start Command: `python TELEGRAM_WEBAPP.py & python TELEGRAM_BOT.py`

5. **Получите URL:**
   - После деплоя Railway предоставит URL типа `https://your-app.railway.app`
   - Скопируйте этот URL

6. **Обновите WEB_APP_URL:**
   - Settings → Variables
   - Добавьте/обновите: `WEB_APP_URL=https://your-app.railway.app`

7. **Настройте Telegram Bot:**
   - Откройте [@BotFather](https://t.me/BotFather)
   - `/setmenubutton`
   - Выберите вашего бота
   - Введите URL: `https://your-app.railway.app`

---

## Render

### Преимущества:
- ✅ Бесплатный тариф
- ✅ Автоматическое развертывание
- ✅ HTTPS из коробки

### Шаги:

1. **Создайте аккаунт на [Render](https://render.com/)**

2. **Создайте Web Service:**
   - New → Web Service
   - Подключите GitHub репозиторий
   - Настройки:
     - **Build Command:** `pip install -r requirements_telegram.txt`
     - **Start Command:** `python TELEGRAM_WEBAPP.py`
     - **Environment:** Python 3

3. **Добавьте переменные окружения:**
   - Environment → Add Environment Variable
   - Добавьте все переменные из раздела "Подготовка"

4. **Создайте Background Worker для бота:**
   - New → Background Worker
   - Подключите тот же репозиторий
   - **Start Command:** `python TELEGRAM_BOT.py`
   - Добавьте те же переменные окружения

5. **Получите URL и настройте:**
   - Скопируйте URL вашего Web Service
   - Обновите `WEB_APP_URL` в переменных окружения
   - Настройте Telegram Bot (см. Railway, шаг 7)

---

## VPS (DigitalOcean, Hetzner и т.д.)

### Преимущества:
- ✅ Полный контроль
- ✅ Нет ограничений бесплатного тарифа
- ✅ Можно использовать свой домен

### Шаги:

1. **Создайте VPS:**
   - Рекомендуется: Ubuntu 22.04 LTS
   - Минимум: 1GB RAM, 1 CPU

2. **Подключитесь к серверу:**
   ```bash
   ssh root@ваш_сервер_ip
   ```

3. **Установите зависимости:**
   ```bash
   apt update
   apt install -y python3 python3-pip git
   ```

4. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/ваш_username/ваш_репозиторий.git
   cd ваш_репозиторий
   ```

5. **Установите Python зависимости:**
   ```bash
   pip3 install -r requirements_telegram.txt
   ```

6. **Создайте файл с переменными окружения:**
   ```bash
   nano .env
   ```
   Добавьте:
   ```
   TELEGRAM_BOT_TOKEN=ваш_токен
   OPENAI_API_KEY=ваш_ключ
   WEB_APP_URL=https://ваш_домен.com
   PORT=8080
   ```

7. **Установите systemd сервисы:**

   Создайте `/etc/systemd/system/telegram-webapp.service`:
   ```ini
   [Unit]
   Description=Telegram Web App Server
   After=network.target

   [Service]
   Type=simple
   User=root
   WorkingDirectory=/root/ваш_репозиторий
   EnvironmentFile=/root/ваш_репозиторий/.env
   ExecStart=/usr/bin/python3 TELEGRAM_WEBAPP_SERVER.py
   Restart=always

   [Install]
   WantedBy=multi-user.target
   ```

   Создайте `/etc/systemd/system/telegram-bot.service`:
   ```ini
   [Unit]
   Description=Telegram Bot
   After=network.target

   [Service]
   Type=simple
   User=root
   WorkingDirectory=/root/ваш_репозиторий
   EnvironmentFile=/root/ваш_репозиторий/.env
   ExecStart=/usr/bin/python3 TELEGRAM_BOT_SERVER.py
   Restart=always

   [Install]
   WantedBy=multi-user.target
   ```

8. **Запустите сервисы:**
   ```bash
   systemctl daemon-reload
   systemctl enable telegram-webapp telegram-bot
   systemctl start telegram-webapp telegram-bot
   ```

9. **Настройте Nginx (для HTTPS):**
   ```bash
   apt install -y nginx certbot python3-certbot-nginx
   ```

   Создайте `/etc/nginx/sites-available/telegram-app`:
   ```nginx
   server {
       listen 80;
       server_name ваш_домен.com;

       location / {
           proxy_pass http://localhost:8080;
           proxy_set_header Host $host;
           proxy_set_header X-Real-IP $remote_addr;
           proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
           proxy_set_header X-Forwarded-Proto $scheme;
       }
   }
   ```

   Активируйте:
   ```bash
   ln -s /etc/nginx/sites-available/telegram-app /etc/nginx/sites-enabled/
   nginx -t
   systemctl restart nginx
   certbot --nginx -d ваш_домен.com
   ```

10. **Проверьте статус:**
    ```bash
    systemctl status telegram-webapp
    systemctl status telegram-bot
    ```

---

## Docker локально

Если хотите запустить локально через Docker:

```bash
# Создайте .env файл
echo "TELEGRAM_BOT_TOKEN=ваш_токен" > .env
echo "OPENAI_API_KEY=ваш_ключ" >> .env
echo "WEB_APP_URL=http://localhost:8080" >> .env

# Запустите
docker-compose up -d

# Проверьте логи
docker-compose logs -f
```

---

## 🔍 Проверка работы

1. Откройте Telegram бота
2. Отправьте `/start`
3. Нажмите кнопку "Открыть приложение"
4. Загрузите изображение и протестируйте

---

## 📝 Примечания

- **Бесплатные платформы** могут иметь ограничения по времени работы (например, Render останавливает бесплатные сервисы после 15 минут бездействия)
- **VPS** - самый надежный вариант для постоянной работы
- **Railway** - хороший баланс между простотой и надежностью

---

## 🆘 Решение проблем

### Бот не отвечает:
- Проверьте `TELEGRAM_BOT_TOKEN`
- Проверьте логи: `docker-compose logs bot` или `journalctl -u telegram-bot`

### Web App не открывается:
- Проверьте `WEB_APP_URL` (должен быть HTTPS)
- Проверьте, что веб-сервер запущен
- Проверьте логи веб-сервера

### ИИ не работает:
- Проверьте `OPENAI_API_KEY`
- Проверьте баланс на OpenAI аккаунте

---

## 📞 Поддержка

Если возникли проблемы, проверьте логи и убедитесь, что все переменные окружения установлены правильно.
