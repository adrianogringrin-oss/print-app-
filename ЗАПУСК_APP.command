#!/bin/bash
# Запуск .app приложения с отладкой

cd "$(dirname "$0")"

echo "Запуск .app приложения..."
echo ""

# Открываем .app через open команду
open "ИзвлечениеПринтов.app"

echo "Приложение должно открыться в новом окне"
echo "Если не открывается, запустите напрямую:"
echo "  /usr/bin/python3 GUI_ТОЛЬКО_PIL.py"
