#!/bin/bash
# Скрипт для запуска программы двойным кликом на macOS

# Переходим в директорию скрипта
cd "$(dirname "$0")"

# Запускаем Python скрипт
python3 запустить.py

# Оставляем окно открытым в случае ошибки
if [ $? -ne 0 ]; then
    echo ""
    echo "Нажмите Enter для выхода..."
    read
fi
